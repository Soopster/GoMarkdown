# Reading Mode Test

This is a regular paragraph. It should have extra spacing after it in reading mode.

This is another paragraph. There should be spacing between these paragraphs.

## Code Block Test

Here's some code:

```go
func hello() {
    fmt.Println("Hello")
    fmt.Println("World")
}
```

And here's more text after the code block. This should have spacing from the paragraph above.

## List Test

Here's a list:

- Item one
- Item two
- Item three

And more text after the list.

## Mixed Content

Regular paragraph before code.

```python
def greet(name):
    print(f"Hello, {name}")
    return True
```

Regular paragraph after code. Should have spacing from paragraph above, but code block should be compact.
